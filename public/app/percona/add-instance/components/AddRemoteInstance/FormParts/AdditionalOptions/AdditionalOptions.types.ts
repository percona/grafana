export enum TablestatOptionsInterface {
  disabled = 'disabled',
  default = 'default',
  custom = 'custom',
}

export enum AutoDiscoveryOptionsInterface {
  disabled = 'disabled',
  custom = 'custom',
}
