import React from 'react';
import { StepType } from '@reactour/tour';

const steps: StepType[] = [{ selector: '[aria-label="Alerting"]', content: <div>THIS IS ALERTING</div> }];

export default steps;
